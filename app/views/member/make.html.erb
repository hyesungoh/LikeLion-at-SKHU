<h1>멤버 설정 페이지</h1>
<hr/>

<h2>멤버 추가하기</h2>
<form action="/save_member" method="get">
    이름 : <input type="text" name="name">
    깃수 : <input type="number" name="term">
    운영진 활동 깃수 : <input type="number" name="istutor" value="0">
    <input type="submit" value="submit">
</form>
<hr/>

<h2>멤버 정보 수정, 삭제하기</h2>
<div class="container">
    <div class="row">
      <% temp = 4 %>
      <% @members.each do |member| %>
          <% if temp != member.term %>
          <hr width = "90%">
          <% end %>
          <div class="col-sm-3">
            <div class="card" style="width: 16rem; margin-top: 1rem; margin-bottom: 1rem;">
              <img class="card-img-top" src="/assets/replace.svg" alt="Card image cap">
              <div class="card-body">
                <form action="/update_member/<%= member.id %>" method="get">
                    <input type="text" name="name" value="<%= member.name %>">
                    <input type="text" name="term" value="<%= member.term %>">
                    <input type="text" name="istutor" value="<%= member.istutor %>">
                    <input type="submit" value="수정확인">
                </form>
                <a href="delete_member/<%= member.id %>"><p>삭제</p></a>
              </div>
            </div>
          </div>
          <% temp = member.term %>
      <% end %>
    </div>
</div>